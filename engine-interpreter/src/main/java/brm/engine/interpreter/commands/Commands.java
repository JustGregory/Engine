package brm.engine.interpreter.commands;
import java.io.DataInputStream;


/**
 * Command static methods.
 * @author Gregory
 */
public class Commands {
  private Commands() {
  }

  public static synchronized final Command fromCode(int i, DataInputStream s) {
    // setup a long switch/case structure for analyzing and returning new
    return null;
  }
}
